#include <iostream>
using namespace std;

namespace all_your_base {
  int convert(unsigned int in_base, string in_digits, unsigned int out_base){
    return 1;
    // int dec = 0;
    // int l = in_digits.length();
    // for (int i = 0; i < l; i++){
    //   int num = (int) in_digits[i];
    //   if ((num >= 48) && (num <= 49)){
    //     num = num - (int)'0';
    //   }
    //   else return 0;
    //   dec = 2*dec + num;
    // }
    // return dec;
  }
}
